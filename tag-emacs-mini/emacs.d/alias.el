;; ~/.emacs.d/alias.el
;; Alias definition file
;; Author : Kuzma Ludovic

;; Replace yes/no by y/n
(defalias 'yes-or-no-p 'y-or-n-p)

;; Some major modes aliases
(defalias 'tm 'text-mode)
(defalias 'hexm 'hexl-mode)
(defalias 'cm 'c-mode)
(defalias 'cpm 'c++-mode)
